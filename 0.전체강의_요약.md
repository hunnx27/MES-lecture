# 실제 공장과 실습의 신호 생성 차이
## 실제 공장
```
PLC/센서 (아두이노/라즈베리파이) 
    ↓ MQTT
서버 (Spring Boot)
```

## 우리 교육
```
Python 시뮬레이터 (PLC/센서 역할 대신)
    ↓ MQTT
서버 (Spring Boot)
```

# 1장 센서 데이터 생성 및 처리
## 1. 시뮬레이터(아두이노 or 파이썬) - 센서데이터 생성 + MQTT 전송 (Producing)
- 입력데이터
  > 온도센서, 진동센서, 압력센서, 작업량
- python : https://claude.ai/public/artifacts/4b07720d-7842-4bc2-bca0-74981d313a9c
- 아두이노 : 아두이노로도 가능할 수 있음.

## 2. MQTT 데이터 수신 및 확인(MQTT Subscribe + Monitoring)
- mqtt explorer 로 들어오는 센서데이터 확인해보기
- CLI 명령으로 확인
> mosquitto_sub -h localhost -t "factory/sensor/data" -v
> mosquitto_sub -h localhost -t "factory/alarm" -v 


## 3. 수집서버(Spring Boot MQTT Subscriber 구현)
- MQTT Subscriber
- MySQL Insert

### ++ 확장 시 추가 구현 사항(선택사항)
1. kafka produce
- kafka를 MySQL 앞단에 두고 insert는 하지 않음
2. kafka consume + mysql write - springboot, nodejs, python(fastapi) 등 선택
- kafka consume하여 mysql에 데이터를 적재하는 서버

# 2장 백엔드 데이터 처리
## 4. API서버(nodejs(express)로 구현)
- API구현
- MySQL 조회 연동

# 최종 데이터 적재 구조
```
[센서] 
   ↓ (Publish)
[MQTT Broker] 
   ↓ (Subscribe)
[수집 서버 : Ingestion Server]
   ↓ (Produce)
[Kafka Broker]
   ↓ (Consume)
[DB 적재 서버 : DB Writer / Consumer Worker]
   ↓
[MySQL]
```

4. 대시보드 구현
- 단순 API호출 및 조회(새로고침방식)

4.1 확장
- 
